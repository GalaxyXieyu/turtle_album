[pytest]
# Limit discovery to the unit test suite.
testpaths =
    tests

python_files =
    test_*.py

# The tests/scripts folder contains ad-hoc API test runners that rely on optional tooling.
norecursedirs =
    tests/scripts
    tests/reports
